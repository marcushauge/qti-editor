import {render, fireEvent, waitFor, screen} from '@testing-library/react'
import React from 'react'
import userEvent from '@testing-library/user-event'

import App from "../../App"


it("create one drag element with associated drop area", () => {
    // render(<App/>)
    // //Upload image
    // //Press button, mark area, press button2
    // const createBtn = screen.getByText("Create drag element")
    // fireEvent.click(createBtn)

    // const canvas = screen.getByTitle("bgCanvas")
    // fireEvent.mouseDown(canvas, {clientX: 0})

    // expect(input.files[0]).toStrictEqual(file)
});

