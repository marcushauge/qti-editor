function DragElement(props) {

    return (
        <div>
            <img src={props.src} width={props.width} height={props.height} alt='asdasdad'></img>
        </div>
    )
}

export default DragElement